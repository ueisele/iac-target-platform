# {{ansible_managed}}

[Unit]
Description=Traefik

[Service]
Type=simple
ExecStart=/usr/local/bin/traefik --consul --consul.endpoint=127.0.0.1:8500
Restart=always

[Install]
WantedBy=multi-user.target
